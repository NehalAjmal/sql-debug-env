spec_version: 1
name: sql_debug_env
type: space
runtime: fastapi
app: server.app:app
port: 7860
description: "An RL environment where an AI agent debugs and fixes broken SQL queries"
tags:
  - openenv
  - sql
  - debugging
  - real-world
tasks:
  - id: task_easy
    difficulty: easy
    description: "Fix a SELECT query with syntax typos"
  - id: task_medium
    difficulty: medium
    description: "Fix a broken JOIN condition"
  - id: task_hard
    difficulty: hard
    description: "Fix a GROUP BY query with wrong HAVING threshold"
  - id: task_expert
    difficulty: expert
    description: "Fix a broken window function query with two bugs"