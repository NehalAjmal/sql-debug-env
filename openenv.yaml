spec_version: 1
name: sql_debug_env
type: space
runtime: fastapi
app: server.app:app
port: 7860
description: "A multi-turn RL environment where an AI agent explores a database via SQL queries to answer business questions — like a real data analyst"
tags:
  - openenv
  - sql
  - multi-turn
  - data-analysis
  - real-world
tasks:
  - id: task_easy
    difficulty: easy
    description: "Count employees per department (1+ queries needed)"
  - id: task_medium
    difficulty: medium
    description: "Find top sales performer in Q1 2024 (2+ queries needed)"
  - id: task_hard
    difficulty: hard
    description: "Calculate project budget to salary ratios by department (3+ queries needed)"
  - id: task_expert
    difficulty: expert
    description: "Find employee with best sales efficiency score (3+ queries needed)"